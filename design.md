# Design System v2.1 — HMG HRD × Hyundai CI

> 이 문서는 사람이 읽는 디자인 시스템 원본이다. 코드가 참조하는 값은 `tokens.js`에 있으며 **두 파일은 항상 함께 수정한다.** 컴포넌트에 색·크기·좌표 하드코딩 금지.
>
> **출처**: ① `C:\HRD\portfolio_mgt\Design.md` (구 디자인 가이드) ② HRD v7 프로토타입 CSS 토큰 (최신 관례)
> ③ 실산출물 `43-1-supplier-v6.pdf` 시각 관례 ④ 현대자동차 CI (Hyundai Blue `#002C5F`, Hyundai Sans)
> v2.1: 전문 디자이너 수준 보완 — 어서션 타이틀·히어로 위계·차트 하이라이트·타이포 대비·서페이스 체계·내러티브 장치

## 1. 무드 & 원칙

- 키워드: **신뢰 · 데이터 정합 · 현대 브랜드 프리미엄**
- **블루 모노크롬 시스템**: Hyundai Blue(네이비) 지배(60~70%), 로열·스카이가 위계. "진할수록 핵심, 옅을수록 보조."
- **어서션 타이틀**: 슬라이드 제목은 분류명이 아니라 결론이다. 킥커=분류, 타이틀=주장(≤22자, 완결 문장형). 제목만 읽어도 보고가 끝나야 한다.
- **히어로 원칙**: 슬라이드마다 주인공은 하나. 핵심 지표 하나를 크게(statHero), 나머지는 로우/서브로 강등. 균등 분할 금지.
- 다크 표지 → 라이트 본문 → (파트마다 다크 디바이더) → 다크 클로징. 모든 슬라이드에 시각 요소 ≥ 1.
- 텍스트 속 핵심 수치는 **볼드 + 브랜드 블루** 인라인 강조 (HRD Key Implication 관례).

## 2. 팔레트

| 역할 | 토큰 | HEX | 출처 | 사용처 |
|---|---|---|---|---|
| Primary | `primary` | `002C5F` | 현대차 CI = HRD `--c-navy` | 다크 배경 기조, 타이틀, 칩, 차트 하이라이트, 히어로 숫자 |
| Primary Mid | `primaryMid` | `123E6B` | 파생 | 다크 위 장식·고스트 넘버 |
| Royal | `royal` | `0070AB` | HRD `--c-royal` | 2시리즈, KI 바, ▲증가, 차트 콜아웃 |
| Sky | `sky` | `00A1C7` | HRD `--c-sky` | 액티브 포인트(슬라이드당 ≤1), 클로징 칩 |
| Steel | `steel` | `AEBFD4` | HRD `--tier3` | **차트 비강조 포인트**, 보조 시리즈 |
| Ice | `ice` | `B8CFE8` | 파생 | 다크 위 보조 텍스트·킥커 |
| Ink(헤딩) | `ink` | `002C5F` | ③ 헤딩=네이비 관례 | 라이트 배경 제목·헤드 |
| Body / Muted | `body`/`muted` | `1F2D3D`/`5C7A8E` | HRD | 본문 / 라벨·캡션 |
| Line | `line` | `DFE4EA` | HRD `--gray-200` | 헤어라인 보더, 로우 구분선 |
| **BG Soft** | `bgSoft` | `F0F4F8` | HRD `--c-bg` | **페이지(슬라이드) 배경** |
| White | `white` | `FFFFFF` | ③ 화이트 카드 관례 | **카드 배경** |
| Surface | `surface` | `E8EFF5` | HRD `--c-surface` | 틴트 강조 셀 |
| Positive / Negative | `pos`/`neg` | `0070AB`/`C4531F` | ③ ▲블루/▼번트 관례 | 증감 전용 (신호등 초록·빨강 금지). GAP 배경 `gapBg FBEDE4` |

## 3. 타이포그래피

**폰트 전략 (3단계):** ① 기본 **Malgun Gothic** + 히어로 숫자 **Malgun Gothic Semilight** ② 열람자 전원 설치 확인 시 **Hyundai Sans Head/Text KR**로 교체(`tokens.js font.face/faceLight`) ③ Pretendard는 웹 프로토타입 전용.

> ⚠ QA 렌더는 Noto 대체 → **텍스트 박스 ~10% 여유 폭** 규칙. Semilight 미설치 환경(구형 Windows)에서는 Regular로 폴백된다.

| 요소 | 토큰 | 크기 | 굵기 | 색 | 자간 |
|---|---|---|---|---|---|
| 표지 타이틀 | `display` | 52pt | Bold | white | -0.5pt |
| 슬라이드 타이틀(어서션) | `title` | 36pt | Bold | ink | -0.25pt |
| **히어로 스탯** | `statHero` | **64pt** | **Light(faceLight)** | primary | — |
| 서브 스탯 | `statSub` | 20pt | Bold | primary | — |
| 카드/섹션 헤더 | `section` | 20pt | Bold | ink | — |
| 아이템 헤드 / 본문 | `itemHead`/`body` | 15/13pt | Bold/Reg | ink/body | — |
| 킥커 | `kicker` | 11pt | Bold | muted(다크: ice) | +2.5pt |
| 캡션·출처 / 페이지 | `caption`/`pageNum` | 10/9pt | Reg | muted | — |

원칙: **크기 대비는 드라마틱하게**(히어로 64 vs 라벨 12.5), 단위·보조어는 크기와 색을 두 단계 낮춘다. 본문 왼쪽 정렬. 제목 행간 1.05~1.1, 본문 1.2~1.3.

## 4. 그리드 & 여백

- 캔버스 13.333"×7.5" · 마진 0.6" · 12컬럼 · 거터 0.2" — 모든 x·폭은 `colX/colW`로만 파생 (**정렬은 계산으로 보장**)
- 세로 존: 킥커 0.58 → 타이틀 0.86 → 콘텐츠 1.95~6.85 → 푸터 7.02
- 블록 간격 0.3~0.5" 일관, 가장자리 0.5" 침범 금지. 밀도 대비를 의도적으로: 빽빽한 데이터 존 옆에 빈 존.

## 5. 서페이스 & 모티프

**서페이스 체계 (③ 실산출물 문법):** 라이트 슬라이드 = `bgSoft` 페이지 + **화이트 카드**(헤어라인 `line` 0.75pt + 미세 그림자 body 10%/blur7/offset1). 다크 슬라이드 = **네이비 그라데이션 이미지**(`assets/bg/dark-gradient.png`, scripts/assets-bg.js 생성 — pptxgenjs 그라데이션 미지원 우회) + 반투명 서클 장식.

- **서클 칩**: 번호 0.42"/아이콘 0.5"(서브 로우 0.36"), 네이비 + 흰 콘텐츠. 클로징 칩은 sky 배경 + **navy 글자**(대비 4.5:1).
- **Key Implication 밴드** *(HRD 시그니처)*: 화이트 카드 + **royal 좌측 바 0.055"** + `KEY IMPLICATION` 라벨(royal, 자간+2). 요약·시사점은 반드시 이 형태.
- **고스트 넘버**: 디바이더의 파트 번호 170pt, `primaryMid` 톤온톤.
- **필 배지**: Tier·상태 표기용 (표 패턴 도입 시).

## 6. 차트 — 차트가 결론을 말하게

- **하이라이트 규칙**: 강조 포인트만 `primary`, 나머지는 `steel` — 빌드 후 `scripts/chart_highlight.py`로 dPt 주입 (pptxgenjs 미지원 우회). 콜아웃(royal 11pt bold)을 카드 우상단에.
- 시리즈 색 순서: primary → royal → sky → steel. 단일 시리즈 범례 숨김, 값 라벨(9pt) 표시, 값 축·그리드 숨김.
- 차트 제목 대신 카드 좌상단 단위 라벨(11pt muted). 네이티브 `addChart`만 — 이미지 차트 금지.
- 커버리지=가로 막대(네이비+`line` 트랙), 구성비=도넛(primary/royal/sky) — 패턴 추가 시.

## 7. 내러티브 장치

- 어서션 타이틀 규칙(§1). 킥커에 파트·분류 표기: `H1 2026 · 상반기 핵심 성과`.
- **섹션 디바이더**를 파트마다 삽입 (`sectionDivider` 패턴) — 덱의 호흡.
- 푸터: 좌 워드마크 / 우 `섹션명 · 페이지번호`. 표지·디바이더·클로징은 푸터 없음.
- 표지(다크 그라데이션)와 클로징(동일 문법)으로 수미상관.

## 8. 금지 규칙

- ❌ 타이틀 밑 액센트 라인, 장식용 색 바 — **예외: KI 좌측 바 1건** (§5, 하우스 시그니처)
- ❌ 본문 가운데 정렬, 텍스트만 있는 슬라이드, 저대비(칩 글자 포함 — 실측 4.5:1 이상)
- ❌ 크림/베이지 배경, 신호등식 초록/빨강 증감, 균등 N분할(히어로 없는 슬라이드)
- ❌ 같은 레이아웃 연속 반복, 박스를 넘치는 텍스트, 라벨형 제목("~현황", "~추이"만 있는 제목)
- ❌ SVG 안의 텍스트 (텍스트는 항상 PPT 네이티브 객체)
